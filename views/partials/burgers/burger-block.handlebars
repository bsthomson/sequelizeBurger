<li>
    <h3 id="burgername">{{burger_name}}

    <button class="change-consumed" data-id="{{id}}" data-newconsumed="{{consumed}}">
        {{#if consumed}}COOK MORE{{else}}YUM YUM{{/if}}
    </button>
    </h3>
</li>