<h1>Burgers BURGERS BURGEEEEEEEEEEEEERS</h1>

<h2>Burgers ready to eat</h2>

<ul>
    {{#each burgers}}
      {{#if consumed}}
        {{> burgers/burger-block consumed=false}}
      {{/if}}
    {{/each}}
</ul>

<h2>Sorry these burgers have been eaten</h2>

<ul>
    {{#each burgers}}
      {{#unless consumed}}
        {{> burgers/burger-block consumed=true}}
      {{/unless}}
    {{/each}}
</ul>

<h2>Cook a burger</h2>
<form class="create-form">

    <div class="form-group">
        <label for "bu">Burger Name:</label>
        <input type="text" id="bu" name="name">
    </div>


    <div class="form-group">
        <label for="consumed">Eaten?</label><br>
        <input type="radio" name="consumed" value="0" checked> Sorry couldn't wait to eat it<br>
        <input type="radio" name="consumed" value="1"> I cooked it just for you
    </div>

    <button type="submit">Cook burger</button>
</form>